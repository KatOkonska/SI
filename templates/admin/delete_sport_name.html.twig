{% extends 'layout_admin.html.twig' %}

{% block title %}{{ 'header.delete_sport'|trans}}{% endblock %}

{% block content %}
<h1>{{ 'header.delete_sport'|trans}}</h1>
<h2>{{'title.delete_sport'|trans}}</h2>

{% if error is defined and error|length %}
{{error }}
{% endif %}

<div>
    {{ form_start(form, { method: 'post', action: path('delete_sport_name', {'id':id})}) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'button.delete'|trans}}" class="button" />
    {{ form_end(form) }}
</div>


{% endblock %}
