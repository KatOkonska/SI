{% extends 'layout_admin.html.twig' %}

{% block title %}{{ 'title.edit_user'|trans }}{% endblock %}

{% block content %}
<h1>{{ 'title.edit_user'|trans }}</h1>
<h2></h2>

{% if error is defined and error|length %}
{{error }}
{% endif %}

<div>
    {{ form_start(form, { method: 'post', action: path('edit_user', {'id':id})}) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'button.change'|trans }}" class="button" />
    {{ form_end(form) }}
</div>

{% endblock %}
